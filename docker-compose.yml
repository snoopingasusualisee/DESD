services:
  db:
    image: mysql:8.0
    restart: unless-stopped
    volumes:
      - mysql_data:/var/lib/mysql
    environment:
      MYSQL_DATABASE: brfn_db
      MYSQL_USER: brfn_user
      MYSQL_PASSWORD: brfn_pass
      MYSQL_ROOT_PASSWORD: brfn_root_pass
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost", "-u", "brfn_user", "-pbrfn_pass"]
      interval: 5s
      timeout: 5s
      retries: 5

  web:
    build:
      context: .
      dockerfile: Dockerfile
      target: production
    restart: unless-stopped
    ports:
      - "8000:8000"
    environment:
      DATABASE_ENGINE: mysql
      DATABASE_NAME: brfn_db
      DATABASE_USER: brfn_user
      DATABASE_PASSWORD: brfn_pass
      DATABASE_HOST: db
      DATABASE_PORT: 3306
      DJANGO_SECRET_KEY: change-me-in-production
      DJANGO_DEBUG: "True"
      DJANGO_ALLOWED_HOSTS: localhost,127.0.0.1,0.0.0.0
    depends_on:
      db:
        condition: service_healthy
    volumes:
      - static_files:/app/staticfiles

volumes:
  mysql_data:
  static_files:
